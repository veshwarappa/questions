SELECT meeting_id, subject, created_at, event_date
FROM public.meeting_records
WHERE subject ILIKE '%testbec journal entry%'
ORDER BY created_at DESC;

SELECT meeting_id, subject, created_at
FROM public.meeting_records
WHERE subject = 'Internal Interview : testbec journal entry'
ORDER BY created_at DESC;
