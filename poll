SELECT 
    id,
    error_type,
    error_category,
    error_code,
    error_message,
    step_name,
    occurred_at
FROM public.errors
WHERE meeting_record_id = 'PUT_MEETING_ID_HERE'
ORDER BY occurred_at DESC;
