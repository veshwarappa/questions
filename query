SELECT
  transcript_available_at,
  transcript_available_at AT TIME ZONE 'UTC' AS as_utc,
  transcript_available_at AT TIME ZONE 'Asia/Tokyo' AS as_tokyo
FROM public.transcripts
WHERE meeting_id = 'AAMkADAy...';
