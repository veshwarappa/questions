ContainerAppConsoleLogs_CL
| where TimeGenerated > ago(5m)
| where Log_s contains "Queuing desk"
   or Log_s contains "Research completed"
   or Log_s contains "Sending email"
| order by TimeGenerated desc

SELECT *
FROM workflow_runs
ORDER BY started_at DESC
LIMIT 20;

SELECT *
FROM desk_reports
ORDER BY generated_at DESC
LIMIT 20;
